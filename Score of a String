class Solution(object):
    def scoreOfString(self, s):
        sum = 0
        l = len(s)-1
        for i in range(l):
            a = ord(s[i])
            b = ord(s[i+1])
            sum = sum + abs(a-b)
        return sum 
